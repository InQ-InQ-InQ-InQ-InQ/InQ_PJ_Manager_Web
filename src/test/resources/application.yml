spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testdb?user=team2
    username: team2
    password: 1212
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
